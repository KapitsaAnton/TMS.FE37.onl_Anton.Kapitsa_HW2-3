ОБЩИЕ КОММЕНТАРИИ К ЗАДАНИЮ:

a) мне кажется, что карточки из нашего текущего ДЗ - это ссылки, при нажатии на которые пользователь провалится далее (в какое-том меню покупки тарифа, например).
Из-за этого я обернул карточки в тэг <a>, а также изменил положение одной из карточке (shadow и "повышение" карточки относительно прочих),
я применил только при наведении на соответствующую карточку (сделал небольшой hover). 

b) для первой части каждого блока (заголовок / линия / текст) я сделал в виде самомтоятельного блока - его можно копировать в следующие секции,
и применять модификаторы (понадобится в последней секции). 

с) я применил flex для карточек, чтобы сделать задание быстрее (предыдущее ДЗ делал без него).


ВОПРОСЫ, КОТОРЫЕ У МЕНЯ ОСТАЛИСЬ:

1) Вопрос по wrapper.
Я же правильно понимаю, что wrapper будет внутри секции, вот так:
    <section class="about">
        <div class="wrapper">
Я так делал, чтобы присвоить секции фон, а внутри него wrapper ограничивает кнтентную область.
Правильно ли я понимаю, что wrapper так и должен работать (т.е. он будет вписываться внутрь каждой section / header / main / footer)?