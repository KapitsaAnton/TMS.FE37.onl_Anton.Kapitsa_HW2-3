ОБЩИЕ КОММЕНТАРИИ К ЗАДАНИЮ:

a) Заливки.
Я добавил заливки к body и .wrapper, чтобы было видно положение / размер нашего блока.
Строка снизу (SMT SMT...) - нужна для того, чтобы было видно, где начнется следующий блок.

b) Заголовки.
При работе использовались заголовки h2, h4, h5 - h3 пропущен намеренно. 
Я понимаю, что это не очень логично в рамках одной работы, но в макете сайта в целом есть h3, 
который используется для заголовка в больших карточках (у нас такой нет), поэтому я сразу брал H4 и далее.

c) Выравнивание элементов.
Если я правильно понял задание, пока мы не делаем адаптив.
Мои блоки пока будут очень так себе сжиматься / перемещаться, если начать менять размер страницы и т.д.
Пока я в это не лез специально - если что-то пропустил, что мы должны были сделать, откомменть пожалуйста.

d) Текст (описание) в карточке (под картинкой).
Если ты посмотришь шаблон, то там текстовка состоит из 5 строк, при этом 2-я строка - одно слово:
adipiscing

Я понимаю, что его можно вынести на отдельную строку через br или заключив в p,
но пока я этого не делал, т.к. не совсем понимаю, зачем.
Lorem - это условный текст, т.е. на его месте должно быть какое-то нормальное описание,
при этом в каждой карточке оно будет разное (т.е. не будет одинакового p на одно слово).


ВОПРОСЫ, КОТОРЫЕ У МЕНЯ ОСТАЛИСЬ:

1) Я оставлял комментарии в css / html файлах, чтобы было проще оринтироваться по структуре кода.

Вопрос: так вообще можно делать или лучше сносить эти комменты и писать классы / делать структура файлов так, 
чтобы они сами пос ебе заменяли комментарии?

2) Вопрос по 3 карточкам. У них ширина 370 px, в шаблоне указан отступ между ними в 30px, всего получается 1170px.
У меня ширина карточек указана напрямую через width (370px), отступы я делаю через margin-right: 
(у последней карточки он отсутствует, я убираю его с помощью псевдокласса :last-child).
Но. У меня этот margin не 30px, а 26px. На эти 26 пикселей я вышел путем проб и ошибок (суть в том, что если 
поставить 27, то 3-я карточка переносится на следующую строку - если 25, то остается зазор между 3-й карточкой
и границей враппера). Т.е. у меня сейчас 370 + 26 + 370 + 26 + 370 = 1162px

Вопрос: я не понимаю, куда делись эти 8px (и почему именно 8). Это я где-то накосячил, 
или нужно обнулить еще какой-то стиль?

3) Я выравнивал в ходе работы все элементы (по высоте) через padding / margin. Так же вырочнены картинки
внутри кружков. Но, там у каждой картинки разный размер отступа (чтобы поместить ее в центр). Я не знал,
как лучше обратиться к разным элементам, в итоге обратился напрямую через id.

Вопрос: это нормальная практика использовать id - или нужно было бы как-то иначе откапывать эти элементы,
чтобы обратиться к ним (я просто не придумал пока ничего другого для этой ситуации)?
